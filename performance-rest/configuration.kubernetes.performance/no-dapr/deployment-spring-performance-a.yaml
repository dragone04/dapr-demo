apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-performance-a
  namespace: development-performance-non-dapr
  labels:
    app: spring-performance-a
spec:
  replicas: 2
  selector:
    matchLabels:
      app: spring-performance-a
  template:
    metadata:
      labels:
        app: spring-performance-a
    spec:
      containers:
        - name: spring-performance-a
          image: docker.io/library/spring-performance-a:0.1.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 9080
          readinessProbe:
            httpGet:
              path: /api/health
              port: 9080
            initialDelaySeconds: 10
            periodSeconds: 30
            successThreshold: 1
            timeoutSeconds: 2
          livenessProbe:
            httpGet:
              path: /api/health
              port: 9080
            failureThreshold: 3
            initialDelaySeconds: 30
            periodSeconds: 15
            successThreshold: 1
          resources:
            requests:
              cpu: "0.2"
              memory: 256Mi
            limits:
              cpu: "0.5"
              memory: 512Mi

---

apiVersion: v1
kind: Service
metadata:
  name: spring-performance-a
  namespace: development-performance-non-dapr
spec:
  ports:
    - port: 9080
      targetPort: 9080
      protocol: TCP
  selector:
    app: spring-performance-a
  sessionAffinity: None
  type: NodePort